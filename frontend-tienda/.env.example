# Base URL of the ResumeAI backend (Nginx load balancer).
VITE_API_URL=http://localhost:8080

# Optional bearer token for the public "customer" identity used to register sales.
# Leave empty to let the backend resolve a seeded customer user without auth.
VITE_CUSTOMER_TOKEN=
